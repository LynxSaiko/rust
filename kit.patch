--- a/Source/JavaScriptCore/API/JSStringRef.cpp
+++ b/Source/JavaScriptCore/API/JSStringRef.cpp
@@ -45,6 +45,8 @@ JSStringRef JSStringCreateWithCharacters(const JSChar* chars, size_t numChars)
     if (!chars)
         return 0;

+    printf("DEBUG: JSStringCreateWithCharacters called with %zu characters\n", numChars);
+
     UString ustring(reinterpret_cast<const UChar*>(chars), numChars);
     return OpaqueJSString::create(ustring).leakRef();
 }

@@ -100,6 +102,8 @@ void JSStringRelease(JSStringRef string)
 {
     if (!string)
         return;

+    printf("DEBUG: JSStringRelease called\n");
+
     static_cast<OpaqueJSString*>(string)->deref();
 }
